{
  "private": true,
  "name": "connectivitycheck",
  "version": "1.0.0",
  "description": "Connectivity checker for Gatekeeper",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "Lukas Reschke <lukas@statuscode.ch>",
  "license": "AGPL",
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}
